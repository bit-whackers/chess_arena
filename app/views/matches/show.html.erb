<h1><%= @match.name %></h1>
<%= link_to 'Edit', edit_match_path(@match), class: 'btn btn-primary' %>
<%= link_to 'Delete', match_path(@match), method: :delete, data: {confirm: 'Are you sure you want to delete this?'},class: 'btn btn-danger' %>
